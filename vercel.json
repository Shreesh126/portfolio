{
  "builds": [
    { "src": "./package.json", "use": "@vercel/static-build", "config": { "distDir": "server/public" } },
    { "src": "server/vercel.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/server/vercel.ts" }
  ]
}